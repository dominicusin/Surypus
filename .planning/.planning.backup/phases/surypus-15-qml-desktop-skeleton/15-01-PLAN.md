---
phase: 15-qml-desktop-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - surypus-api/src/Surypus/API/Server.hs
  - surypus-api/src/Surypus/JWT/Token.hs
autonomous: true
requirements:
  - QML-01
  - QML-04
must_haves:
  truths:
    - "Login endpoint returns a real signed JWT token with expiry"
    - "Auth middleware validates JWT tokens from Authorization header"
    - "Expired/invalid tokens return 401"
    - "JWT signing key is configurable via environment variable"
  artifacts:
    - path: "surypus-api/src/Surypus/JWT/Token.hs"
      provides: "JWT signing/verification using jose library"
      exports:
        - "generateToken :: Pool -> User -> IO Text"
        - "verifyToken :: Text -> IO (Either String UserClaims)"
        - "UserClaims { userId, username, roles, exp }"
      min_lines: 80
    - path: "surypus-api/src/Surypus/API/Server.hs"
      provides: "Updated handleLogin and authMiddleware"
      contains: "import qualified Surypus.JWT.Token as JWT"
  key_links:
    - from: "Server.hs handleLogin"
      to: "JWT.Token.generateToken"
      pattern: "JWT.generateToken"
    - from: "Server.hs authMiddleware"
      to: "JWT.Token.verifyToken"
      pattern: "JWT.verifyToken"
    - from: "authMiddleware"
      to: "401 response"
      pattern: "err401"
---

<objective>
**Implement real JWT authentication in the backend.**

Replace the current stub token (`"jwt-token-"+uid`) with cryptographically signed JSON Web Tokens using the existing `jose-0.10` library. Update the auth middleware to decode and verify tokens on every protected request.

**Purpose:** Satisfy QML-01 (login with JWT credentials) by returning real JWTs from the API. Fix the auth middleware to actually validate tokens rather than just check header presence.

**Output:**
- `surypus-api/src/Surypus/JWT/Token.hs` — JWT signing + verification module
- Updated `surypus-api/src/Surypus/API/Server.hs` — new handleLogin and authMiddleware
- Updated `surypus-api/surypus-api.cabal` — add new module to exposed-modules
</objective>

<execution_context>
@/home/domini/src/My/Surypus/.opencode/get-shit-done/workflows/execute-plan.md
@/home/domini/src/My/Surypus/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/surypus-15-qml-desktop-skeleton/15-CONTEXT.md

# Backend types and existing auth flow
@src/DAL/Types.hs (lines 524-544 — LoginRequest/LoginResponse)
@surypus-api/src/Surypus/API/Server.hs (lines 80-95, 107-143, 183-200 — authMiddleware, SurypusApi type, handleLogin)
@surypus-api/src/Surypus/DAL/Mutations.hs (lines 453-470 — authenticateUser)
@surypus-api/surypus-api.cabal (lines 12-13 — exposed-modules to update)
@src/Surypus/JWT.hs — existing JWT module for reference patterns

<interfaces>
From Surypus.CoreTypes (re-exported via src/Surypus/CoreTypes.hs):
- data LoginRequest = LoginRequest { lrUsername :: !Text, lrPassword :: !Text }
- data LoginResponse = LoginResponse { lrToken :: !Text, lrRefreshToken :: !(Maybe Text), lrUserId :: !Int64, lrExpiresIn :: !Int }
- data User — has userId, username, role fields
  (see src/DAL/Types.hs for full User definition)
- type Pool = Pool from Hasql
- data QueryResult a = QuerySuccess a | QueryError Text

From Server.hs:
- data Env = Env { envPool :: Pool, envLogger :: Log.Logger, envWSHandler :: Maybe WS.WebSocketHandler }
- handleLogin :: Env -> LoginRequest -> Handler LoginResponse
- authMiddleware :: Application -> Application
- data AuthenticatedUser = AuthenticatedUser { userId :: Int64, username :: Text, userRoles :: [Text] }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create JWT signing/verification module</name>
  <files>
    surypus-api/src/Surypus/JWT/Token.hs
    surypus-api/test/Surypus/JWT/TokenSpec.hs
    surypus-api/surypus-api.cabal
  </files>
  <read_first>
    Discovery needed: jose-0.10 API. Run `cabal info jose` or examine `stack exec -- ghc-pkg field jose exposed-modules` to find the module layout. Expected API: `Jose.Jwt` provides `encode`/`decode`, `Jose.Jwa` provides `JwsAlg(HS256)`. Use a signing key from `SURYPUS_JWT_SECRET` env var (default to a dev secret). Create RS256 or HS256 JWT with claims: sub (userId), name (username), role, exp (1h from now), iat.

    The module should expose:
    - `generateToken :: Pool -> User -> IO Text` — creates signed JWT
    - `verifyToken :: Text -> IO (Either String UserClaims)` — validates and decodes
    - `data UserClaims = UserClaims { userId :: Int64, username :: Text, roles :: [Text], exp :: Integer }`
  </read_first>
  <behavior>
    - Test 1: `generateToken` produces a non-empty dotted string (3 segments separated by '.')
    - Test 2: `verifyToken` on a valid token returns `Right UserClaims` with matching userId
    - Test 3: `verifyToken` on an expired token returns `Left "token expired"`
    - Test 4: `verifyToken` on a tampered token returns `Left` with error
  </behavior>
  <action>
    Create `surypus-api/src/Surypus/JWT/Token.hs` providing JWT generation and verification using `jose-0.10`.

    1. Read `jose-0.10` API via `stack exec -- ghc-pkg field jose exposed-modules` to confirm module names.
    2. Implement `generateToken`:
       - Read signing key from `SURYPUS_JWT_SECRET` env var (with compile-time default `"dev-secret-change-in-production"`).
       - Use HS256 (HMAC-SHA256) algorithm.
       - Set claims: `sub`=userId, `name`=username, `role`=role, `iat`=now, `exp`=now+3600s.
    3. Implement `verifyToken`:
       - Decode JWT, validate HMAC signature, check expiry.
       - Return `Right UserClaims` on success, `Left errorMsg` on failure.
    4. Update `surypus-api.cabal`:
       - Add `Surypus.JWT.Token` to `exposed-modules`.
       - Ensure `jose`, `cryptonite`, `time` are in `build-depends`.
    5. Create test file with hspec tests for the four behaviors above.
  </action>
  <verify>
    <automated>stack test surypus-api --test-arguments='-p JWT'</automated>
  </verify>
  <done>
    - `Surypus.JWT.Token` module exists with `generateToken` and `verifyToken`
    - Tests pass: valid token round-trips, expired/tampered tokens rejected
  </done>
</task>

<task type="auto">
  <name>Task 2: Update handleLogin to return real JWT</name>
  <files>
    surypus-api/src/Surypus/API/Server.hs
  </files>
  <read_first>
    Read `Server.hs` line 107 (SurypusApi type) and line 145-200 (server + handleLogin). The login endpoint is at `"login"` serving `POST` with `LoginRequest` body, returning `LoginResponse`. The current `handleLogin` at line 185-200 calls `DAL.Mutations.authenticateUser` then returns a fake token.

    Import `Surypus.JWT.Token (generateToken)`. Update `handleLogin` to:
    - After `QuerySuccess (Just user)` (line 191), call `JWT.generateToken` with the user
    - Set `lrToken` to the real JWT
    - Set `lrExpiresIn` to 3600
    - Set `lrRefreshToken` to `Nothing` for now
  </read_first>
  <action>
    Update `handleLogin` in `Server.hs`:
    1. Add import: `import qualified Surypus.JWT.Token as JWT`
    2. In the `QuerySuccess (Just user)` branch, replace the fake token with:
       `token <- liftIO $ JWT.generateToken (envPool env) user`
    3. Return `LoginResponse` with the real JWT, `lrExpiresIn = 3600`.
    4. Do NOT change the `QuerySuccess Nothing` or `QueryError` branches (they already return 401/500 correctly).
  </action>
  <verify>
    <automated>stack build surypus-api</automated>
  </verify>
  <done>
    - `POST /api/v1/login` with valid credentials returns a real JWT in `lrToken`
    - The token decodes to valid JSON Web Token format (three base64url segments)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update authMiddleware to validate JWT</name>
  <files>
    surypus-api/src/Surypus/API/Server.hs
  </files>
  <read_first>
    Read `Server.hs` line 80-95 (authMiddleware). Currently it only checks if Authorization header exists — it does NOT validate the token content. Update it to:
    1. Extract Bearer token from Authorization header
    2. Call `JWT.verifyToken` on the token
    3. On success: proceed with request (optionally add user info to request for handlers)
    4. On failure: return 401

    The middleware ships with WAI `Application` type, so it takes `Application -> Application`. Extract the token from the `Authorization` header (strip "Bearer " prefix), decode with `JWT.verifyToken`, and on failure respond 401.

    Since `verifyToken` returns `IO (Either String UserClaims)`, you'll need `liftIO` inside the WAI Application.
  </read_first>
  <action>
    Update `authMiddleware` in `Server.hs`:
    1. Add import: `import qualified Data.Text as DT`
    2. Replace the current authMiddleware body:
       - Extract Authorization header value
       - Strip "Bearer " prefix (7 chars) from the value
       - Call `JWT.verifyToken` with the stripped token
       - On `Left err`: respond 401 with JSON body `{"error": "Invalid or expired token"}`
       - On `Right _`: proceed with `app req respond`
    3. The `/api/v1/login` endpoint MUST be excluded from auth (it's how users get tokens). Add a path check: if the request path is `/api/v1/login` or starts with `/api/v1/auth/`, skip auth.
  </action>
  <verify>
    <automated>stack build surypus-api</automated>
  </verify>
  <done>
    - Requests to `/api/v1/login` do NOT require Authorization header
    - Requests to protected endpoints WITHOUT a valid JWT return 401
    - Requests to protected endpoints WITH a valid JWT proceed normally
    - Invalid/expired/tampered tokens return 401
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Unauthenticated login request crosses here |
| API → client | JWT token sent over HTTP (not HTTPS in dev) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | S | JWT signing | mitigate | Signing key read from `SURYPUS_JWT_SECRET` env var, never hardcoded in production |
| T-15-02 | T | authMiddleware | mitigate | Token expiry enforced (1h), expired tokens rejected |
| T-15-03 | I | authMiddleware | mitigate | HMAC signature verified on every request, tampered tokens rejected |
| T-15-04 | E | login endpoint | accept | Dev-only; production would use HTTPS. Token transmission in clear only on localhost. |
</threat_model>

<verification>
- `stack build surypus-api` succeeds
- `POST /api/v1/login` with valid `{lrUsername, lrPassword}` returns 200 with real JWT
- `POST /api/v1/login` with bad credentials returns 401
- `GET /api/v1/bills` without Authorization header returns 401
- `GET /api/v1/bills` with valid JWT returns 200
- `GET /api/v1/bills` with expired/tampered JWT returns 401
</verification>

<success_criteria>
- Backend returns signed JWTs from login endpoint
- Auth middleware validates tokens on protected routes
- Invalid/expired tokens correctly rejected with 401
- Login endpoint remains unprotected (no auth required)
</success_criteria>

<output>
After completion, create `.planning/phases/surypus-15-qml-desktop-skeleton/15-01-SUMMARY.md`
</output>
