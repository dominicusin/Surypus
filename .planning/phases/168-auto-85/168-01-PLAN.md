---
id: "168-01"
phase: "168"
name: "Notification Email Lookup"
waves:
  - verify
depends_on: []
files_modified:
  - surypus-api/src/Surypus/API/Notifications.hs
autonomous: true
---

# Plan 168-01: Notification Email Lookup

## Overview

Verify notification sending uses real DB email lookup from `users` table.

## Tasks

### Task 1: Verify lookupUserEmail

<read_first>
- surypus-api/src/Surypus/API/Notifications.hs
</read_first>

<action>
Verify `lookupUserEmail` queries `users` table by user ID, and `sendEmailNotification` uses the real email with fallback.
</action>

<acceptance_criteria>
- [ ] `lookupUserEmail` queries `users` table by user ID
- [ ] `sendEmailNotification` uses real email from DB
- [ ] Falls back to default email if user not found
</acceptance_criteria>

## Verification Criteria

### must_haves
- [ ] Real DB email lookup confirmed
- [ ] Fallback behavior confirmed

## Wave Assignments
- **Wave 1 (verify):** Task 1
