---
id: "166-01"
phase: "166"
name: "Real Login with DB Auth"
waves:
  - verify
depends_on: []
files_modified:
  - surypus-api/src/Surypus/DAL/Mutations.hs
  - surypus-api/src/Surypus/API/Server.hs
autonomous: true
---

# Plan 166-01: Real Login with DB Auth

## Overview

Verify the existing implementation of real PostgreSQL authentication using `crypt()` password verification against the `users` table.

Requirements: AUTH-01, AUTH-02, AUTH-03

## Tasks

### Task 1: Verify authenticateUser SQL

<read_first>
- .planning/ROADMAP.md (phase 166 success criteria)
- surypus-api/src/Surypus/DAL/Mutations.hs (authenticateUser implementation)
</read_first>

<action>
Read the `authenticateUser` function in `surypus-api/src/Surypus/DAL/Mutations.hs`.

Verify that:
1. The SQL query targets the `users` table (not `usr`)
2. The password comparison uses `crypt($2, password_hash)` or equivalent bcrypt approach
3. The function returns `QuerySuccess (Just User)` on valid credentials
4. The function returns `QuerySuccess Nothing` on invalid credentials
</action>

<acceptance_criteria>
- [ ] `authenticateUser` queries the `users` table (confirmed by file read)
- [ ] Password comparison uses crypt() for bcrypt support
- [ ] Valid credentials return `QuerySuccess (Just User)`
- [ ] Invalid credentials return `QuerySuccess Nothing`
</acceptance_criteria>

### Task 2: Verify Server Integration

<read_first>
- surypus-api/src/Surypus/API/Server.hs (handleLogin function)
</read_first>

<action>
Read `handleLogin` in `surypus-api/src/Surypus/API/Server.hs`. Verify it properly calls `authenticateUser` and handles the response.
</action>

<acceptance_criteria>
- [ ] `handleLogin` calls `authenticateUser` with correct parameters
- [ ] Response handling is correct for both valid and invalid credentials
</acceptance_criteria>

### Task 3: Verify Build

<read_first>
- N/A — run build command
</read_first>

<action>
Run `stack build` to verify the package compiles without errors.
</action>

<acceptance_criteria>
- [ ] `stack build` exits with code 0
</acceptance_criteria>

## Verification Criteria

### must_haves
- [ ] `authenticateUser` queries `users` table with crypt() password verification
- [ ] Returns proper QuerySuccess variants for valid/invalid credentials
- [ ] `stack build` passes

## Wave Assignments

- **Wave 1 (verify):** Tasks 1-3 — verify existing implementation
