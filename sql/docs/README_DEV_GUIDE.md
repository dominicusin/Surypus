# Development Guide (RBACCanon)

- Run stack build/test to verify compilation.
- Migrate SQL using V001+ migrations, generated from Haskell domain changes.
- Use DSL.hs as a wire format for SQL generation.
