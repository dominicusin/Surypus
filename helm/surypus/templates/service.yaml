apiVersion: v1
kind: Service
metadata:
  name: {{ include "surypus.fullname" . }}
  labels:
    {{- include "surypus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "surypus.selectorLabels" . | nindent 4 }}