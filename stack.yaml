resolver: lts-22.21

system-ghc: true

nix:
  enable: false

packages:
  - .

ghc-options:
  "$locals": -Wall

allow-newer: false

flags:
  hashtables:
    portable: true
    sse42: false
    unsafe-tricks: false
  warp:
    x509: false
